# Mandatory and nice configuration steps for git

```
git config --global user.email "sporniket.studio@gmail.com"
dsporn@debbie:~/Projects/zz-workstation/sporniket-workstation/00-git-config$ git config --global user.name "David SPORN"
```

