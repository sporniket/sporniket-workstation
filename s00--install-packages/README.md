# Install additionnal packages

This step install all packages deemed necessary for performing intended tasks (e.g. git, build essentials,... ), but also any package that I find nice and ends up using regularly (e.g. one of those fancy `du` replacements)

I will maintain the list of package of interest in the file `LIST.txt`.
